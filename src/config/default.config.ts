import { Config } from './index'

const defaultConfig: Config = {
  src: 'App.tsx',
  container: 'root',
  root: '',
  public: 'public',
  SSR: false,
  env: 'production'
}

export default defaultConfig